[package]
name = "tests-integration"
version = "2.0.0-rc2"
edition = "2021"
publish = false

[dev-dependencies]
oca-rs = {path = "../oca", features = ["local-references"]}
overlay-file = { path = "../overlay-file" }

[[test]]
name = "build_from_ocafile"
path = "build_from_ocafile.rs"
